import 'package:flutter/material.dart';
class PruebasStart extends StatelessWidget {
  const PruebasStart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Lista de Pruebas")
    );
  }
}
